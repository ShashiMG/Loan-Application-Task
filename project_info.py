# Define the project name
project_name = "loan-applications"

# Define database connection information (if applicable)
db_host = "localhost"
db_port = 5432  # Replace with your database port
db_name = "loan_db"
db_user = "your_db_user"
db_password = "your_db_password"

# Define API endpoints (if applicable)
loan_application_api = f"http://localhost:5000/api/submit-application"
user_registration_api = f"http://localhost:5000/api/register"
user_login_api = f"http://localhost:5000/api/login"
